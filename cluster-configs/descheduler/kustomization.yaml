apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: descheduler-configs

resources:
- descheduler-operator.yaml
- descheduler-instance.yaml

commonLabels:
  config.openshift.io/component: descheduler
  config.openshift.io/managed-by: gitops
