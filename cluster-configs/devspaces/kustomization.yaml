apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: devspaces-configs

resources:
  - devspaces-operator.yaml
  - devspaces-instance.yaml

commonLabels:
  config.openshift.io/component: devspaces
  config.openshift.io/managed-by: gitops
