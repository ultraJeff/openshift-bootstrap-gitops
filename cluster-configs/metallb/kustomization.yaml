apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: metallb-configs

resources:
- metallb-operator.yaml
- metallb-instance.yaml
- metallb-config.yaml

commonLabels:
  config.openshift.io/component: metallb
  config.openshift.io/managed-by: gitops
