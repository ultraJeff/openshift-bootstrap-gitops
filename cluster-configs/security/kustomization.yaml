apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: security-configs

resources:
- admin-rbac.yaml
- cluster-oauth.yaml
- ultra-lab-ca.yaml
- proxy-trusted-ca.yaml
- keycloak-oidc-eso.yaml
- keycloak-rbac.yaml

commonLabels:
  config.openshift.io/component: security
  config.openshift.io/managed-by: gitops
