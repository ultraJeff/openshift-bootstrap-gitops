apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: serverless-configs

resources:
  - serverless-operator.yaml
  - logic-operator.yaml
  - sonataflow-namespace.yaml
  - sonataflow-platform.yaml

commonLabels:
  config.openshift.io/component: serverless
  config.openshift.io/managed-by: gitops
