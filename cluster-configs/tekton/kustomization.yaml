apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: tekton-configs

resources:
  - tekton-operator.yaml

commonLabels:
  config.openshift.io/component: tekton
  config.openshift.io/managed-by: gitops
