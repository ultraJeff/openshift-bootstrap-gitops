---
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: resource-test-app-build
  namespace: resource-test
  labels:
    app: resource-test-app
spec:
  output:
    to:
      kind: ImageStreamTag
      name: resource-test-app:latest
  source:
    type: Binary
    binary: {}
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  triggers:
  - type: ConfigChange

---
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: resource-test-app
  namespace: resource-test
  labels:
    app: resource-test-app
spec:
  lookupPolicy:
    local: true
